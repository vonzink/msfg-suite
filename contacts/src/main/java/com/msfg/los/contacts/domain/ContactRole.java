package com.msfg.los.contacts.domain;

/** Loan contact roster roles — mirrors the FE's 8 display strings (FE maps labels). */
public enum ContactRole {
    LISTING_AGENT,
    SELLING_AGENT,
    ESCROW_OFFICER,
    TITLE_COMPANY,
    INSURANCE_AGENT,
    ATTORNEY,
    APPRAISER,
    OTHER
}
