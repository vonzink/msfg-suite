services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: msfg_los
      POSTGRES_USER: los
      POSTGRES_PASSWORD: los
    ports: ["5432:5432"]
    volumes: ["./data/pg:/var/lib/postgresql/data"]
